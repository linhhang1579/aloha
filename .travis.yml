language: node_js
node_js:
- "10"

branches:
  only:
  - master

addons:
  chrome: stable

before_script:
  - npm install -g @angular/cli@latest

script:
- ng test
- ng build --prod --base-href https://aloha2309-dev.herokuapp.com

deploy:
  provider: heroku
  app:
    master: aloha2309-dev
  api_key: $HEROKU_AUTH_TOKENd
